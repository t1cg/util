logger.test.js has the jest test for all the helper functions

output.js has the tests to make sure the output is correct
- when you make a change you can run "node output.js -u" to change the golden file